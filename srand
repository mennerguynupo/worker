#include <iostream>
#include <cstdlib>
#include <ctime>
using namespace std;

int main() {
    srand(time(0)); // 初始化随机种子
    for (int i = 0; i < 5; i++) {
        cout << "随机数: " << rand() % 100 + 1 << endl; // 生成1到100之间的随机数
    }
    return 0;
}
